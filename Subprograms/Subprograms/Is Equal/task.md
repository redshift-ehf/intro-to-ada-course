## Exercise: Equality function

Write a function that reports whether two integers are equal.

```adasnippet
function Is_Equal (A, B : Integer) return Boolean;
```

Ada's `=` already returns a `Boolean`, so the body is one line — the point is the shape of a
function returning something other than a number.

Press **Check** when you are done.
